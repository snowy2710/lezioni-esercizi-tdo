#esempi

show(cars)
#questo comando "show" mostra le variabili e i valori di un dataset"cars"

hist(cars$dist)
#"hist" msotra un grafico, istogramma, del dataset "cars" in funzione della variabile "dist"

plot(cars)
plot(cars$speed, cars$dist)
#"plot" mostra grafico a puntini in relazione delle due variabili x(speed), e y(dist)

help(show)
help(hist)
#usare comando "help" per far comparire la spiegazione del dato
